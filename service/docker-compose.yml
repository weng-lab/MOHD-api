services:
  db:
    image: postgres
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - 5433:5432
  api:
    image: mohd-api
    environment:
      POSTGRES_URL: "postgresql://postgres:postgres@db:5433/postgres"
    ports:
      - 3000:3000
